<div class="header">
  <div class="large-2 columns float-left"><a href="/"><img src="/assets/stop-trump.png" /></a></div>
  <div class="large-10 columns float-left"><div class="user-login">
<ul>
                <%if user_signed_in? %>
                  <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
                <%else%>
                  <li><%= link_to "Log In/Sign Up", new_user_session_path%></li>
                <% end %>
            </ul>
  </div></div>
  </div>
  <% if !notice.blank? %>
      <p class="callout alert"><%= notice %></p>
      <p class="alert"><%= alert %></p>
  <% end %>