<!DOCTYPE html>
<html>
    <head>
        <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
    </head>
    <body>
        <p><%=image_tag attachments["stop-trump-small.png"].url%>
            <h3>Dear&nbsp;<%= @first_name %></h3>
            <h3>
                The following message was sent to
                <%= @target_first_name %>
                <%= @target_last_name %>
                on your behalf by Stop Trump
            </h3>

            <h3 style="font-weight:normal"><% @message_text.split("|").each do |m| %>
                <p><%= m %></p>
                <%end%>
            </h3>
            <h3>Your Congressional Representatives</h3>
            <h3>
                <table cellpadding="5" cellspacing="5">
                    <tr>
                        <th>Name</th>
                        <th>Phone</th>
                        <th>Email Link</th>
                    </tr>
                    <% @action_array[1,@action_array.length-1].each do |r| %>
                    <tr>
                        <td><%= r[:rep_name] %></td>
                        <td><%= r[:rep_phone] %></td>
                        <% if r[:rep_email].nil? %>
                        <td>Email Not Available</td>
                    <% else %>
                        <td><%= link_to "Click to Send an Email Message", r[:rep_email] %></td>
                        <% end  %>
                    </tr>
                    <%#= r[:rep_name] + ' ' + r[:rep_phone] %>
                    <% end %>
                </table>
            </h3>

        </h3>
    </body>
</html>
