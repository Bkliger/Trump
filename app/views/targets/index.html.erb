<div class="row">
<div class="large-12 columns friends">
<section>
  <div id="targets-index">
    <div class="container-fluid">
<br/>
      <div class="col-sm-5">
        <h1 class="display-3">Friends and Family</h1>
        <%= link_to "New Friends and Family", new_target_path, class: "button secondary" %>
  <%if current_user.admin?  %>
      <%= link_to "Messages" , messages_path, class: "button secondary" %>
      <%= link_to "Representatives" , reps_path, class: "button secondary" %>
      <%= link_to "Reporting" , report_target_path, class: "button secondary" %>
  <% end %>
        <hr>

          <table>
            <thead>
            <tr>
              <th width:"100">Name</th>
              <th width:"100">Status</th>
              <th width:"100">Actions</th>
            </tr>
            <tbody>
              <% @targets.each do |t| %>
            <tr>
            <td><%= t.first_name + " " + t.last_name %></td>
            <td><%=t.status%></td>
            <td><%= link_to "", delete_target_path(t), method: :delete, class: "fa fa-trash",  data: {confirm: 'Are you sure you want to delete this Friend/Family?'}  %>
              &nbsp <%= link_to "", edit_target_path(t.slug), class: "fa fa-pencil" %>
            </td>
            </tr>
                      <% end %>
            </tbody>
            </table>

        </hr>
        <%= will_paginate @targets, renderer: FoundationPagination::Rails %>
        <%= page_entries_info @targets, model: "Friends and Family" %>
      </div>
    </div>
  </div>
</section>
</div>
</div>
